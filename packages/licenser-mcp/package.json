{
  "name": "@gloo/licenser-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Model Context Protocol server for the Licenser platform — lets an agent issue/manage licenses, activations, products, plans, releases and run validations.",
  "type": "module",
  "bin": {
    "licenser-mcp": "dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json && node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@supabase/supabase-js": "^2.45.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20"
  }
}
